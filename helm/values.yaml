# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  registry: "ghcr.io"
  repository: balu1000/actions
  tag: "1.0.1-9311d15-linux-amd64"
  os: linux
  arch: amd64

secret:
  name: kbot
  env: TELE_TOKEN  
  key: token
securityContext:
  privileged: true  